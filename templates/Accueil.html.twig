{% extends "base.html.twig" %}
{% block body %}
    <div>
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
Bienvenue sur LeMeilleurCoin !


{% endblock %}
